{
  "name": "sistem-informasi-rt-5",
  "version": "3.11.0",
  "private": true,
  "description": "SISTEM INFORMASI RT 5 - PWA layanan warga (statis + Supabase). AMAN, BERSIH, MODERN, TRANSPARAN DAN EFISIEN.",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "preview:free": "node server.js --dist",
    "build": "node scripts/build.js",
    "build:free": "node scripts/build.js --tier free"
  },
  "devDependencies": {
    "terser": "^5.31.0"
  },
  "engines": {
    "node": ">=16"
  }
}

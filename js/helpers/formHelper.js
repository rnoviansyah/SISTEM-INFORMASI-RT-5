// ============================================================
// formHelper.js - Generate Form Inputs Dinamis
// ============================================================

export function generateFormInputs(containerId, menu, rowData, session) {
  // ... (salin dari app.js: generateFormInputs)
  // Gunakan parameter session untuk akses role, nik, dll.
}

export function sanitizeFormData(sheetName, formData) {
  // ... (salin dari app.js: sanitizeFormData)
}

export function validateDynamicForm(menu, payload, session) {
  // ... (salin dari table.js: validateDynamicForm)
}
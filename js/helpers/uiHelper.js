// ============================================================
// uiHelper.js - UI Utilities (Toast, Modal, Loading)
// ============================================================

export function showUIToast(message, type = 'auto') {
  // ... (salin dari app.js: showUIToast)
}

export function showUIConfirm(text, onConfirm, title = "Konfirmasi Tindakan") {
  // ... (salin dari app.js: showUIConfirm)
}

export function setBtnLoading(btn, isLoading, customText = 'Menyimpan...') {
  // ... (salin dari app.js: setBtnLoading)
}